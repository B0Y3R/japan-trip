// ------------------------------------------------------------
// Copy this file to `config.js` and paste your key below.
// Needs the "Maps JavaScript API" enabled on the key/project.
//
// `config.js` is gitignored by default. To deploy to GitHub Pages, the live
// page needs the key, so remove the `config.js` line from `.gitignore` and
// commit it — it's referrer-restricted, so it's safe to ship.
// ------------------------------------------------------------
window.GOOGLE_MAPS_API_KEY = "YOUR_MAPS_JS_API_KEY_HERE";
